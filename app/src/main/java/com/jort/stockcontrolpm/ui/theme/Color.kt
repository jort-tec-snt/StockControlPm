package com.jort.stockcontrolpm.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta principal STOCKCONTROL ──────────────────────────────────────────
val Primary         = Color(0xFF0F5132) // Acciones principales, header, CTA
val PrimaryMid      = Color(0xFF166534) // Hover/pressed del primary
val PrimaryLight    = Color(0xFFD4EDDA) // Fondos de chips activos, tints

val Accent          = Color(0xFFE8A825) // Alertas de pago, QR, advertencias
val AccentLight     = Color(0xFFFEF3C7) // Fondo chips de advertencia

val Success         = Color(0xFF16A34A) // Stock saludable, confirmaciones
val SuccessLight    = Color(0xFFD4EDDA) // Badge stock OK

val Warning         = Color(0xFFD97706) // Stock bajo, vencimientos próximos
val WarningLight    = Color(0xFFFEF3C7) // Badge stock bajo

val Error           = Color(0xFFDC2626) // Agotado, eliminación, errores
val ErrorLight      = Color(0xFFFEE2E2) // Badge agotado, fondos destructivos

// ── Fondos y superficies ───────────────────────────────────────────────────
val Bg              = Color(0xFFF8FAF9) // Fondo general de pantalla
val Surface         = Color(0xFFFFFFFF) // Cards, inputs, modales

// ── Texto ──────────────────────────────────────────────────────────────────
val TextPrimary     = Color(0xFF1C2428) // Títulos, valores numéricos
val TextSecondary   = Color(0xFF6C757D) // Labels, descripciones
val TextMuted       = Color(0xFF9CA3AF) // Placeholders, metadata

// ── Separadores ───────────────────────────────────────────────────────────
val Divider         = Color(0xFFE5E7EB) // Separadores, bordes de input
val BorderLight     = Color(0xFFE5E7EB)
